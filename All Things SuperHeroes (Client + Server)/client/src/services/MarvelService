import api from './api'

export default {
  findChar (character) {
    return api().post('characters', character)
  },
  findSeries (series) {
    return api().post('series', series)
  },
  findEvent (event) {
    return api().post('events', event)
  }
}
